# mPower_Rag 依赖文件（v1.1.0 - 功能增强版）
# 创建时间: 2026-02-23 16:47
# 更新时间: 2026-03-10 13:30

# ========== 核心依赖 ==========

# API 框架
fastapi==0.109.0
uvicorn==0.27.0
python-multipart==0.0.6
pydantic==2.5.3
pydantic-settings==2.1.0

# HTTP 客户端
httpx==0.26.0
requests==2.31.0
aiohttp==3.9.1

# ========== 数据处理 ==========

# 基础库（兼容Windows环境）
numpy>=1.20.0,<2.0.0
pandas>=1.3.0,<2.0.0

# ========== LLM 相关 ==========

# OpenAI 客户端
openai==1.10.0

# LangChain（Python 3.11 兼容）
langchain==0.1.0
langchain-community==0.0.10
langchain-openai==0.0.2
langchain-core==0.1.10

# ========== 向量数据库 ==========

# Qdrant 客户端
qdrant-client==1.6.4

# ========== 文档解析（增强） ==========

# PDF 解析
PyPDF2==3.0.1
pymupdf==1.23.8

# DOCX 解析
python-docx==1.1.0

# XLSX 解析
openpyxl==3.1.2

# PPTX 解析（新增）
python-pptx==0.6.23

# RTF 解析（新增）
striprtf==0.0.26

# Markdown 解析
markdown==3.4.3

# HTML 解析（用于Markdown转换）
beautifulsoup4==4.12.2
lxml==5.1.0

# XML 解析（新增）
xmltodict==0.13.0

# ========== OCR 支持（新增） ==========

# OCR 引擎
pytesseract==0.3.10
Pillow==10.2.0

# 注意：需要安装 Tesseract-OCR
# Windows: https://github.com/UB-Mannheim/tesseract/wiki
# Linux: sudo apt-get install tesseract-ocr
# Mac: brew install tesseract

# ========== 文件类型检测（新增） ==========

# 文件类型识别
python-magic==0.4.27
python-magic-bin==0.4.14; sys_platform == 'win32'

# ========== 前端 ==========

# Streamlit
streamlit==1.31.0

# ========== 工具库 ==========

# 环境变量
python-dotenv==1.0.0

# 日志
loguru==0.7.2

# 重试机制
tenacity==8.2.3

# ========== 重排序（Python 3.11 可用）==========

# 交叉编码器重排序
sentence-transformers==2.2.2

# 中文分词
jieba==0.42.1

# ========== 测试 ==========

# pytest
pytest==7.4.4
pytest-asyncio==0.23.3

# 代码检查
ruff==0.1.9

# ========== 缓存机制（Redis） ==========

# Redis 客户端
redis==5.0.1

# ========== 监控 ==========

# Prometheus 客户端
prometheus-client==0.19.0

# ========== 可视化 ==========

# Plotly（图表库）
plotly==5.18.0
plotly-express==0.4.17

# ========== 注意事项 ==========

# 1. LangChain 相关包已包含，支持 Python 3.11
# 2. 重排序功能需要 sentence-transformers
#    - 需要联网下载模型（首次使用）
# 3. OCR 功能需要额外安装 Tesseract-OCR
#    - Windows: 下载安装包
#    - Linux: sudo apt-get install tesseract-ocr tesseract-ocr-chi-sim
#    - Mac: brew install tesseract tesseract-lang
# 4. 如果安装失败，可以尝试：
#    pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt
#    或
#    pip install -i https://mirrors.aliyun.com/pypi/simple/ -r requirements.txt
